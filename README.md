# Steps to successfully run this Docker Server

- Clone this repository
- Keep it in any directory you want
- Go to that directory and Run 
```bash
docker compose up --build
```
The server starts.

> Only works on Linux Server Machines.
